---
import aktieansvar from '../assets/clients/AktieAnsvar-logo-ligg.png'
import akzonobel from '../assets/clients/akzonobel.png'
import gp from '../assets/clients/GP.png'
import scania from '../assets/clients/scania.png'
import stena from '../assets/clients/stena.png'
import svt from '../assets/clients/svt.png'
import volvo from '../assets/clients/Volvo.png'
import akelius from '../assets/clients/akelius.png'
import skf from '../assets/clients/skf.png'
---

<aside class='neu-box'>
  <div
    ><img
      width={250}
      height={(250 * 112) / 500}
      src={aktieansvar}
      alt='Aktie-ansvar'
    /></div
  >
  <div
    ><img
      width={250}
      height={(250 * 97) / 500}
      src={akzonobel}
      alt='Akzo Nobel'
    /></div
  >
  <div
    ><img
      width={250}
      height={(250 * 78) / 500}
      src={gp}
      alt='Göteborgs-posten'
    /></div
  >
  <div
    ><img
      width={250}
      height={(250 * 58) / 500}
      src={scania}
      alt='Scania'
    /></div
  >
  <div
    ><img width={250} height={(250 * 124) / 300} src={stena} alt='Stena' /></div
  >
  <div><img width={250} height={(250 * 124) / 500} src={svt} alt='Svt' /></div>
  <div
    ><img
      width={250}
      height={(250 * 165) / 1200}
      src={volvo}
      alt='Volvo'
    /></div
  >
  <div
    ><img
      width={250}
      height={(250 * 128) / 500}
      src={akelius}
      alt='Akelius'
      style='padding-inline: 1rem'
    />
  </div>
  <div
    ><img
      width={250}
      height={(250 * 95) / 500}
      src={skf}
      alt='SKF'
      style='padding-inline: 0.5rem'
    />
  </div>
</aside>
<style lang='scss'>
  aside {
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0.8;
    // grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    grid-gap: 1rem;
    margin-block: 3rem;
    padding-block: 1rem;
    border-top: 3px solid hsl(30, 4%, 100%, 0.5);
    border-bottom: 3px solid hsl(30, 4%, 20%, 0.5);
    img {
      display: block;
      max-width: 100%;
      max-height: 2rem;
      object-fit: scale-down;
      filter: grayscale(1);
    }
  }
  html.dark-theme aside {
    border-color: hsl(30, 4%, 100%, 0.3);
    border-bottom: none;
    img {
      filter: grayscale(1) invert(1) opacity(0.8);
    }
  }
</style>
